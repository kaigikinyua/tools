from django.db import models

# Create your models here.
"""
    class Authentication(models.Model):
        username=models.CharField(max_length=50)
        last_logged=models.DateField(auto_now=True, auto_now_add=True)
        
"""