cat read a file -- cat filename 
    read a file in scroll mode -- cat filename | more or cat filename | less

grep - scan a file for a particular match
    cat hist.txt | grep mellon - search for mellon in hist.txt file
    flags:
        -v shows the lines that do not match with the word
        -c dispay the number of matching lines
        -n show the matching line and its number
        -i match both uppercase and lowercase letters
        -I show the filename with the matching string

sort - sorting contents of a file alphabetically
    flags:
        -r - revese sorting
        -n sort numerically
        -f  case sensitive sorting

Processes:

ps - list Processes